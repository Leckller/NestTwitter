import styled from "styled-components";

export const StyledFormSearch = styled.form`

  label {
    display: flex;
    flex-wrap: nowrap;
    gap: 8px;
    width: 100%;
    border-radius: 16px;
    border: solid 1px black;
    padding: 8px;

    input {
      width: 90%;
      border: none;
      outline: none;
    }

  }

`;